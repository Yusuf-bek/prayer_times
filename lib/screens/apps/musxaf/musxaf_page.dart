import 'package:flutter/material.dart';

class MusxafPage extends StatelessWidget {
  const MusxafPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}